CREATE TABLE PRODUCTOS (
  ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  NOMBRE VARCHAR2(160) NOT NULL,
  CATEGORIA VARCHAR2(80) NOT NULL,
  PRECIO NUMBER(12, 2) NOT NULL,
  STOCK NUMBER(10) NOT NULL,
  ACTIVO NUMBER(1) DEFAULT 1 NOT NULL
);

ALTER TABLE PRODUCTOS
  ADD CONSTRAINT CK_PRODUCTOS_ACTIVO CHECK (ACTIVO IN (0, 1));

ALTER TABLE PRODUCTOS
  ADD CONSTRAINT CK_PRODUCTOS_PRECIO CHECK (PRECIO >= 0);

ALTER TABLE PRODUCTOS
  ADD CONSTRAINT CK_PRODUCTOS_STOCK CHECK (STOCK >= 0);
